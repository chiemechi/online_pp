<header class="header">
      <nav class="nav">
        <ul class="header_menu">
          <li class="nav-link">〇〇さん</li>
          <li class="header_menu_title"><%= link_to "kanban", root_path, class: "nav-link" %></li>
          <li><%= link_to "リストを作成", new_list_path, class: "nav-link listNew" %></li>
          <li><%= link_to "サインアウト", destroy_user_session_path, class: "nav-link", method: :delete %></li>
        </ul>
            </ul>
          </li>
        </ul>
      </nav>
    </header>

    <div class="topPage">
  <div class="listWrapper">
    <% @lists.each do |list| %>
      <div class="list">
        <div class="list_header">
          <h2 class="list_header_title"><%= list.title %></h2>
          <div class="list_header_action">
            <%= link_to(content_tag(:i, '', class: 'fas fa-pen'), edit_list_path(list)) %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>